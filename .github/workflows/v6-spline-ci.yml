name: V6 Spline CI Legacy Snapshot

on:
  workflow_dispatch:

jobs:
  legacy-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy single-file snapshot retained for history; active V6 verification uses v6-spline-ci-chunks.yml."
